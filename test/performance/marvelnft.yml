config:
  target: 'http://localhost:3000'
  phases:
    - duration: 10
      arrivalRate: 50
  ensure:
    maxErrorRate: 1
    max: 500
 
scenarios:
  - name: 'Check application health.'
    flow:
      - get:
          url: '/ping'
          expect:
            - statusCode: 200
